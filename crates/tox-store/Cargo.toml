[package]
name = "tox-store"
description = "SQLite persistence: posts, comments, friends, profile, kv settings"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }

[dev-dependencies]
