[package]
name = "tox-core"
description = "Safe Rust wrapper around c-toxcore (session lifecycle, event loop, callbacks to channel)"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
tox-ffi = { path = "../tox-ffi" }
hex = "0.4"
ed25519-dalek = "2"
num-bigint = "0.4"
num-traits = "0.2"

[dev-dependencies]
hex = "0.4"
